import React from "react";
import "./Toolbar.css";
import { Container, Row, Col } from 'react-bootstrap';


function Toolbar({ todo, index, toggleTodo }) {
  return (
    <Col className="toolbar" >sdf
    </Col>
  );
}

export default Toolbar;